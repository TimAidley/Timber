<!doctype html>
<html>
  <head>
    <title>{{ site.title }}</title>
    <link rel="stylesheet" href="{{ site.basePath }}/assets/theme.css" />
  </head>
  <body class="acme">
    <h1>{{ page.title }}</h1>
    {{ content }}
  </body>
</html>
