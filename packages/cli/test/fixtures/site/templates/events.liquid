<!doctype html><html><head><title>{{ page.title }}</title></head><body class="events"><h1>{{ page.title }}</h1>{% if page.startDate %}<time>{{ page.startDate }}</time>{% endif %}{{ content }}</body></html>
