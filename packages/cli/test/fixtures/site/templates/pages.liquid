<!doctype html><html><head><title>{{ seo.title }}</title><meta name="description" content="{{ seo.description }}"><link rel="canonical" href="{{ seo.canonical }}"></head><body class="pages"><h1>{{ page.title }}</h1>{{ content }}<ul class="events-list">{% for e in collections.events %}<li><a href="{{ e.url }}">{{ e.title }}</a> {{ e.startDate }}</li>{% endfor %}</ul></body></html>
