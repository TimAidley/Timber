<!doctype html>
<html>
  <head><title>{{ title }} · {{ site.title }}</title></head>
  <body class="eleven">
    <h1>{{ title }}</h1>
    <p class="byline">by {{ metadata.author }}</p>
    <main>{{ content }}</main>
  </body>
</html>
