{
  "name": "@timber/eleventy-compat",
  "version": "0.0.0",
  "description": "Timber's Eleventy-theme compatibility layer: a mechanical import transform + ecosystem filter shims, so Tier-A (Liquid-authored) Eleventy themes can be imported and rendered by Timber's generator. Pure and isomorphic.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@timber/generator": "workspace:*",
    "@timber/jekyll-compat": "workspace:*",
    "liquidjs": "^10.19.0"
  }
}
